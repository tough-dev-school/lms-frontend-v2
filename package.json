{
  "name": "lms-frontend-v2",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint-staged": "lint-staged --concurrent false"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0",
    "prettier": "^3.1.0"
  }
}
