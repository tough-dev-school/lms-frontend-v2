{
  "name": "lms-frontend-v2",
  "private": true,
  "packageManager": "pnpm@9.11.0",
  "scripts": {
    "dev": "pnpm --filter lms-frontend-v2 dev",
    "build": "pnpm --filter lms-frontend-v2 build",
    "check": "pnpm -r check",
    "fix": "pnpm -r fix",
    "test": "pnpm -r test:unit",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.1.0",
    "lint-staged": "^15.1.0"
  },
  "volta": {
    "node": "22.14.0"
  }
}
