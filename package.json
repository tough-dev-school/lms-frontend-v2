{
  "name": "lms-frontend-v2",
  "type": "module",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "test:lint": "pnpm -r run test:lint",
    "test:format": "pnpm -r run test:format",
    "test:type": "pnpm -r run test:type",
    "test:unit": "pnpm -r run test:unit",
    "prepare": "husky install",
    "lint-staged": "lint-staged --concurrent false"
  },
  "engines": {
    "node": "24.3.0",
    "pnpm": "10.12.4"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.1.0",
    "prettier": "^3.1.0"
  }
}
